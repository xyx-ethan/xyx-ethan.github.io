name: Diophantine7 Round5 Four-Descent Archive

on:
  workflow_dispatch:

jobs:
  archived:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Magma V2.29-9 FourDescent does not accept CrvHyp over number fields.'
