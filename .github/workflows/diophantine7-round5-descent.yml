name: Diophantine7 Round5 Descent Recon Archive

on:
  workflow_dispatch:

jobs:
  archived:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Archived after the Q-squareclass descent computation.'
