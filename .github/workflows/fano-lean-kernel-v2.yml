name: Fano Lean kernel certificate v2 (archived)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  archived:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Superseded by the versioned final kernel workflow."
