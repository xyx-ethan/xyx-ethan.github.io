name: Diophantine7 Round5 Residual Search Archive

on:
  workflow_dispatch:

jobs:
  archived:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Residual-cover searches were launched separately; this trigger is archived to avoid duplicate runs.'
